# Storebrand's custom typeface is licensed and must not be redistributed.
# The stylesheet loads it from assets.storebrand.no instead.
assets/fonts/
*.woff
*.woff2

# Seed SQL carries real colleague names and this repo is public.
# Run it in Supabase, keep it off GitHub.
*.local.sql
supabase/seed.local.sql

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
